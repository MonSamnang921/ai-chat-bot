const path = require('path');
const express = require('express');
const app = express();

// ជំនួស app.use(express.static('public')) ដោយ៖
app.use(express.static(__dirname));

app.get('/', (req, res) => {
    res.sendFile(path.join(__dirname, 'index.html'));
});
